# kwagenda

React Native (Expo) iOS app.

## Run

```bash
npm install
npm start
```

Scan the QR code or open the Expo URL in [Expo Go](https://expo.dev/go).

For a shareable tunnel URL (needed when not on the same network):

```bash
npx expo start --tunnel
```
